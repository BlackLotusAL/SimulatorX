[tool.pytest.ini_options]
testpaths = ["src/test"]
pythonpath = ["src"]
addopts = "-ra --import-mode=importlib -p pytest_plugin"
markers = ["integration: real services, sockets, child processes or native builds", "native_sdk: requires a supported platform and C compiler"]
