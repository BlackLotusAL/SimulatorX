[pytest]
pythonpath = ../../src ..
testpaths = tests
addopts = -ra --import-mode=importlib -p no:pytest_plugin -p demo.common.collection
markers = integration: real devices, HTTP or pytest subprocess
