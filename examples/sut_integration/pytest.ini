[pytest]
testpaths = tests
pythonpath = . ../../src
addopts = -ra --import-mode=importlib
markers =
    integration: real services, sockets, child processes or native builds
    business: SUT business acceptance through real device protocols
    linux_sdk: requires Linux and a C compiler
